- defaultTab: nodes
  description: "Create a database user specific table and select permissions"
  executionEnabled: true
  group: Databases/Create/User
  loglevel: INFO
  name: Create Database User
  # Boilerplate
  nodeFilterEditable: false
  plugins:
    ExecutionLifecycle: null
  scheduleEnabled: true
  # End boilerplate
  sequence:
    commands:
    - description: Select Database
      exec: echo "Select a database..."
    - description: Select Permissions
      exec: echo "Select permissions..."
    - description: Second step (inline-script)
      fileExtension: .sh
      interpreterArgsQuoted: false
      script: |
        #!/bin/bash

        echo "step two"
      scriptInterpreter: /bin/bash
    - description: Third step (command step)
      exec: echo "step three"  